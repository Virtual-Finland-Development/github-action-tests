name: Test environment values flow with a push
on:
  push:

jobs:
  test-dev:
    uses: lsipii/test/.github/workflows/test.yml@main
    with:
      environment: dev
    secrets: inherit
  test-staging:
    uses: lsipii/test/.github/workflows/test.yml@main
    with:
      environment: staging
    secrets: inherit
